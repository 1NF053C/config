# apptainer.conf settings
# set these in your apptainer.conf file on host, might be found in `/etc/apptainer/apptainer.conf`

mount home = no

#bind path = /etc/hosts # disable until we need container to access localhost